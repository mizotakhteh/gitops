apiVersion: v1
kind: Service
metadata:
  name: book
  labels:
    app: book
spec:
  type: ClusterIP
  ports:
    - name: tcp
      protocol: TCP
      port: 5432
      targetPort: tcp
  selector:
    app: postgres